# The server loads environment variables from the project-root `.env` file
# (see `.env.example` at the repository root for the full variable list).
#
# A local `.env` at this path is optional and only used for overrides.
PORT=4000
NODE_ENV=development
